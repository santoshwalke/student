export class studentModel {
    constructor(first_name: string, last_name: string, address: string, email: string, gender: string, ip_address: string) {
        first_name = first_name;
        last_name = last_name;
        address = address;
        email = email;
        gender = gender;
        ip_address = ip_address;
    }
}